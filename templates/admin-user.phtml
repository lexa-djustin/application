<div class="table-wrapper">
    <div class="table-title">
        <div class="row">
            <div class="col-sm-5">
                <h2>User <b>Management</b></h2>
            </div>
            <div class="col-sm-7">
                <a href="/calculator/admin-user?action=create" class="btn btn-primary"><i class="material-icons">&#xE147;</i>
                    <span>Add New User</span></a>
            </div>
        </div>
    </div>
    <?php foreach ($this->errors as $error) : ?>
        <div class="alert alert-danger" role="alert">
            <?php echo $error ?>
        </div>
    <?php endforeach; ?>
    <table class="table table-striped table-hover">
        <thead>
        <tr>
            <th>id</th>
            <th>Name</th>
            <th>Email</th>
            <th>Department</th>
            <th>Role</th>
            <th>Action</th>
        </tr>
        </thead>
        <tbody>
        <?php foreach ($this->users as $user): ?>
            <tr>
                <td><?php echo $user['id'] ?></td>
                <td><a href="#"><?php echo $user['name'] ?></a></td>
                <td><a href="#"><?php echo $user['email'] ?></a></td>
                <td><?php echo $user['department'] ?></td>
                <td><?php echo $user['role'] ?></td>
                <td>
                    <a href="/calculator/admin-user?action=edit&id=<?php echo $user['id'] ?>" class="settings" title="Settings"
                       data-toggle="tooltip"><i
                                class="material-icons">&#xE8B8;</i></a>
                    <a href="javascript:;"
                       data-href="/calculator/admin-user?action=delete&id=<?php echo $user['id'] ?>" class="delete delete-user"
                       title="Delete"
                       data-toggle="tooltip"><i
                                class="material-icons">&#xE5C9;</i></a>
                </td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>
</div>

<script>
    $(document).ready(function () {
        $('.delete-user').on('click', function () {
            var href = $(this).data('href');

            if (confirm('Are you sure?')) {
                location.href = href;
            }
        });
    });
</script>